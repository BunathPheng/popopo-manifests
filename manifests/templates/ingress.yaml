---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: popopo                  # Ingress name
  labels:
    app: popopo
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "popopo.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: popopo               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
